# PatternLoop
PatternLoop: Self-improving AI agents that run locally forever.  Ollama-powered agent OS. Give a goal + examples → compiles reusable LoopSpec, then runs smart ReAct loop (plan → act → evaluate → adapt) until success.  Zero cloud  Fully local  5 ready agents: research, inbox, content lead, coder.  Personal AI employee that gets smarter every run.
